vector:
	和数组类似，拥有连续的内存空间，起始地址不变，能高效的随机存取，时间复杂度未o(1),
	插入删除会造成内存块的拷贝，时间复杂度为o(n),
	数组中内存空间不够时，会重新申请内存空间，进行内存拷贝
	游标支持+=，+ <等操作
list:
	双向链表实现，存储空间是不连续的。只能通过指针访问数据，随机存储是没有效率的，时间复杂度o(n)
	能高效的进行插入删除
	游标不支持支持+=，+ <等操作
高效随机存取，不在乎插入删除效率，使用vector
大量随机插入删除，不关心随机存取 使用list