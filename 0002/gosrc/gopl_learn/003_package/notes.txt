包 是函数和数据的集合，用package保留字定义一个包，文件名不需要与包名一致。
包名使用小写字母，go包可以由多个文件组成。
小写字母开始的函数不可导出，为私有函数，
大写字母开始的函数可以导出，在包外使用 
使用包中函数  packagename.Function()
使用私有函数  会编译报错
包中的新类型，全局变量也遵循大小写首字母规则。大写公有，小写私有。不仅仅是ASCII，大写希腊语 古埃及语都是可以